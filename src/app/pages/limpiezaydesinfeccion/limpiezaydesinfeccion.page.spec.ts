import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LimpiezaydesinfeccionPage } from './limpiezaydesinfeccion.page';

describe('LimpiezaydesinfeccionPage', () => {
  let component: LimpiezaydesinfeccionPage;
  let fixture: ComponentFixture<LimpiezaydesinfeccionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(LimpiezaydesinfeccionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
